plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow'
}

repositories {
    mavenCentral()
    google()
    maven {
        url 'https://oss.sonatype.org/content/repositories/snapshots'
    }
}

dependencies {

    // Gson for JSON parsing
    implementation 'com.google.code.gson:gson:2.13.1'

    // Guava for EventBus
    implementation 'com.google.guava:guava:33.0.0-jre'

    // Logging
    implementation 'org.apache.logging.log4j:log4j-slf4j2-impl:2.23.1'
    implementation 'org.apache.logging.log4j:log4j-api:2.23.1'
    implementation 'org.apache.logging.log4j:log4j-core:2.25.3'
    // Reflections
    implementation 'org.reflections:reflections:0.9.12'

    //Utils
    implementation 'org.apache.commons:commons-lang3:3.18.0'

    //Look and Feel
    implementation 'com.formdev:flatlaf:3.5.2'

    // SQLite driver
    implementation 'org.xerial:sqlite-jdbc:3.51.0.0'

    // JDBI 3 - the real winner for Java + SQL in 2025
    implementation 'org.jdbi:jdbi3-core:3.47.0'
    implementation 'org.jdbi:jdbi3-sqlobject:3.47.0'
    implementation 'org.jdbi:jdbi3-jackson2:3.47.0'

    implementation 'org.zeromq:jeromq:0.6.0'
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.17.2'

    // YAML
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.18.0'     // or latest 2.18.x / 2.19.x / 2.21.x
    implementation 'com.fasterxml.jackson.dataformat:jackson-dataformat-yaml:2.18.0'
    implementation 'com.fasterxml.jackson.datatype:jackson-datatype-jsr310:2.18.0'


    /// Embedded light weight web service
    // Core Javalin (already have this)
    implementation 'io.javalin:javalin:7.0.1'

    // OpenAPI support – match version to Javalin major (7.0.x here)
    def openapiVersion = '7.0.1'

    // Runtime plugin to serve /openapi.json
    implementation "io.javalin.community.openapi:javalin-openapi-plugin:${openapiVersion}"

    // Optional: Swagger UI integration
    implementation "io.javalin.community.openapi:javalin-swagger-plugin:${openapiVersion}"

    // Optional: ReDoc UI integration
    // implementation "io.javalin.community.openapi:javalin-redoc-plugin:${openapiVersion}"

    // Annotation processor (required for @OpenApi to generate spec at compile time)
    annotationProcessor "io.javalin.community.openapi:openapi-annotation-processor:${openapiVersion}"
    // If using Kotlin: kapt instead of annotationProcessor
}


java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

tasks.withType(JavaCompile) {
    options.compilerArgs << '-parameters'
}

application {
    mainClass = 'elite.vault.Ingest'
}

sourceSets {
    main {
        resources {
            srcDirs = ['src/main/resources']
            include '**/*.json'
            include '**/*.conf'
            include '**/*.css'
            include '**/*.png'
            include '**/*.wav'
            include '**/*.xml'
            include 'META-INF/MANIFEST.MF'
            include '**/db-migration/*.sql'
            include '**/scripts/*.sh'
            include 'version.txt'
        }
    }
}

shadowJar {
    archiveFileName = 'elite_vault.jar'
    manifest {
        attributes 'Main-Class': 'elite.vault.Ingest'
    }
    mergeServiceFiles()
    destinationDirectory = file("${rootProject.projectDir}/distribution")
}

// Ensure only the fat JAR is produced and used by assemble/build
tasks.named('jar') {
    enabled = false
}
tasks.named('assemble') {
    dependsOn tasks.shadowJar
}